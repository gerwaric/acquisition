# Governed surfaces — the register under C79

C79 (`CONTEXT.md`): surfaces GGG does not sanction — the trade site, the
forums, third-party feeds — are governed inputs, never runtime
dependencies, and permission attaches to the access method. This file
is the register that ruling names. One row per surface: its status,
its terms exposure, the access method permission is granted for, the
cadence, and what it yields. An entry changes only by a commit that
says why. The daemon never touches any of these; no store read, plan
compile or apply depends on one; tooling fetches one only from an
official export or with the permission its row records; what a surface
yields lands as `T<n>` claims (`docs/design/trade-ground-truth.md`,
master-side) or reviewed reference data with sources cited (C68). A
surface used as an *effect* (posting) needs its own boundary session
first — that is the parked publishing item in `CONTEXT.md`. The API
reference (`pathofexile.com/developer/docs`) and the API itself are
sanctioned and need no row; what GGG's own client writes into item
notes reaches us through the API and is cited as `game:` evidence.

| Surface | Status | Terms exposure | Access method | Cadence | Yields |
| --- | --- | --- | --- | --- | --- |
| The trade site, `pathofexile.com/trade` (its price-note formats, currency vocabulary, listing display, the About page) | **registered 2026-09-03**; first dated read 2026-09-03: the owner saved `/trade/about` in a browser (kept locally in `pricing-info/`, gitignored — GGG's text is quoted in claims, never committed whole); the agent read the saved copy | GGG's site terms; not part of the API reference; no published terms for automated reads | `browser` — a human reads it and records a dated observation. **No automated fetch**; a tool may only *propose* rows from what a human recorded | on demand: the bulk-exchange item list (parked, `decisions/pricing.md`), the seller-account search as the oracle for the listing state | dated observations cited in `crates/acquisition-plan/reference/currency-v1.toml`; T1–T4 (`docs/design/trade-ground-truth.md`) |
| The trade site's internal data endpoints (`/api/trade/data/static`, `/data/stats`, `/data/items`, `/data/filters`) and the search page's bundle | **rejected as a tooling source** (packet §1(d), 2026-09-03); **captured once by the owner 2026-09-12** for the item-search research (`search/trade-query/MANIFEST.md`): the four data responses are committed dated under `search/trade-query/data/` so captures diff across leagues (owner's call, index rule 4); the page save and its JavaScript stay local, and what they say is extracted (`grammar.json`) | not in the API reference; undocumented internal endpoints | `browser` only, as evidence for a human-committed row or a dated capture; tooling fetches nothing | on demand, one dated capture per reading | proposals for the currency table, never the table itself; the query grammar and vocabularies for the search design (`search/README.md`) |
| The forum shop threads, `pathofexile.com/forum` | known; **not used by the spike**. The owner posts by hand: one character item linked and priced on 2026-09-03 (T7, T8); automated posting and bumping are parked (`CONTEXT.md`) | credentialed session (POESESSID) for posting; reading is public | `browser` — the owner posts what `shop render` writes to stdout; tooling sends nothing (C74) | one hand experiment per open matrix cell (`PRICING-SLICE.md`, "Observations still open") | `T<n>` claims on link codes, resolution at post time, indexing and visibility |
| PoE Wiki, `poewiki.net` (the forum-listing guide) | **corroboration only**, read 2026-09-03 from a copy the owner saved (`pricing-info/`, gitignored; CC BY-NC 3.0) | the wiki's licence | `browser`; a third party's reading of GGG's surfaces, never authority | — | T14, cited as wiki, dated |
| Procurement, `github.com/Procurement-PoE/Procurement` | **corroboration only**, read 2026-09-03 through the GitHub API (last release 1.29.2, 2022-12-22) | Artistic-2.0 | a public code read; a third party's prior reading of the same folklore as the C++ app | — | T15; nothing the C++ code did not already say |
| Third-party price feeds (market and suggested prices) | **not registered** — parked (`decisions/pricing.md`) | each feed's own terms | — | — | — |
| RePoE (`github.com/repoe-fork/repoe`, the parser; `repoe-fork/poe1`, the PoE1 export; `repoe-fork/poe2`, not cloned) | known; used by the C++ app on `master` at runtime (fetched from the Pages host, no attribution), **not by the spike**; **cloned by the owner 2026-09-13** beside this repository for the item-search research (`search/repoe/MANIFEST.md`: remote, commit, version) — the owner, verbatim: "Instead of doing a fetch, I can clone both repos locally" | MIT code; the generated data is GGG's: "Contents of generated files … are owned by Grinding Gear Games and shall not be used or published without being in accordance with their terms of use" (`LICENSE.md`); the export's generator itself reads the trade site's stats endpoint | `clone` — the owner clones and pulls; tooling reads the local checkout only, cited by commit; **no fetch by tooling, never a runtime dependency**; derived extracts committed with the source cited, whole data files never | at the owner's pull, one dated capture per reading | the stat-id spine behind display text, base and class taxonomy, mod ranges and weights (`search/repoe/`); **the class table the search ships** (`crates/acquisition-search/reference/classes-v1.toml`, base names and the game's class names, regenerated by `tools/class-table.py` at each pull and reviewed as a diff; C106, 2026-09-23) |
| Path of Building Community (`github.com/PathOfBuildingCommunity/PathOfBuilding`) | known; the C++ app's "Copy for Path of Building" targets its import format (`master`, `src/item.cpp`); **cloned by the owner 2026-09-13** beside this repository, for one experiment inside `search/repoe/` (the trade-stat-id derivation) and as corroboration | MIT code ("Copyright (c) 2016 David Gowor"); every generated data file is headed "Game data (c) Grinding Gear Games" | `clone` — as the RePoE row: the owner clones, tooling reads locally, cited by commit; no fetch, no runtime dependency | at the owner's pull | the trade-stat-id hash recipe (`src/Export/Scripts/mods.lua`), the mod and base tables as corroboration, the import grammar |
| GGG's item-filter documentation, `pathofexile.com/item-filter/about` (the loot-filter syntax, its conditions and vocabulary) | known; **registered 2026-09-13** for the item-search research (`search/item-filter/`); GGG's own published documentation of a game feature, not the API reference, so it takes a row | GGG's site terms; a public documentation page | `browser` — the owner saves a dated copy under the track's `raw/`; the extraction (`data/conditions.csv`) is what is committed; tooling fetches nothing | one dated capture per reading | the third GGG-authored naming of the item predicates, beside the API and the trade filters; class names for the taxonomy |
| Awakened PoE Trade, `github.com/SnosMe/awakened-poe-trade` (the item-text-to-trade-query tool; its stats dataset and generator) | known; **registered 2026-09-13** for one read inside `search/prior-art/` — how a maintained tool gives a displayed line its stat identity, handles several ids per line and follows patches; a third party's prior reading of the same site and game data, corroboration and design evidence, never authority | MIT code (to confirm at the clone); its generated data derives from GGG's | `clone` — as the RePoE row: the owner clones beside this repository, tooling reads the checkout only, cited by commit; no fetch, no runtime dependency; extracts committed with the source cited, its data files never | at the owner's pull | the stat-entry shape, the ambiguity and rename handling (`search/prior-art/`) |
