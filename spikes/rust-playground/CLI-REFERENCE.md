# `acq` — command reference

Generated by `crates/acquisition-cli/tests/cli_reference.rs` from the binary's own `--help`, every subcommand walked from the root; a golden file the test keeps equal to the binary. Do not edit: change the help string in `crates/acquisition-cli/src/main.rs` and regenerate with `ACQ_UPDATE_FIXTURES=1 cargo test -p acquisition-cli --test cli_reference`. The README's tour shows the shape; this is every verb's semantics. `--json` and `--account` are global — every verb takes them — and are listed once, under `acq`. The MCP server's tools are `MCP-REFERENCE.md`.

## `acq`

```text
Acquisition playground CLI (mock provider by default; ACQ_GGG=1 talks to real GGG)

Usage: acq [OPTIONS] <COMMAND>

Commands:
  auth          Log in via OAuth (mock provider, or real GGG with ACQ_GGG=1). The login completes only once its own profile job lands the account uuid (C50); the mock's page accepts any username, so a second account is one login apart
  profile       The account profile (account:profile)
  characters    List characters on the logged-in account. A route's first use queues a visible `probe` job — one HEAD that learns the policy and the account's current counters before anything real is sent (C20)
  character     Fetch one character with its equipment and inventory
  leagues       List the account's leagues
  accounts      Accounts this machine has logged into (the store's index; no daemon)
  tabs          Tabs of a league, from the shared store (no daemon round-trip)
  items         Items in the shared store
  store         The shared store itself (what the daemon writes; every frontend reads)
  stashes       List stash tabs for a league: a second rate-limit policy, paced in parallel with the character routes
  stash         Fetch one stash tab (or one substash of a map/unique tab)
  refresh       Refresh tabs: one stash-list request, then one `stash` child job per selected tab. Selection is explicit — there is no default. (The ad-hoc `--tabs`/`--all` kind and the plan path are two doors to one task; C76 rules the direction.)
  policy        The per-account sync policy: the declared coverage and freshness that `acq refresh --plan` compiles into requests
  submit        Submit any job kind by hand
  demo          Submit a burst of fetch jobs against the mock's 5-per-10 s policy and watch the rate limiter queue them (the ETAs are the limiter's prediction, corrected by headers)
  dash          Live dashboard (TUI): rate limiter state (enter expands a policy: bucket state, the observed X-Rate-Limit headers, per-endpoint sends), job queue, HTTP sends, recent errors, a rails halt in red. With --json, prints one snapshot and exits
  version       What this build is (C84): the package version, the shared-contract revision the daemon handshake compares — a digest over the protocol and store crates, the root manifest and the lock, never a git commit — and the sibling `acqd` a job command would start, as found on disk (path, length, modification time; no hash — the run record hashes it). This reports the candidate; `acq daemon status` reports the daemon running. `--json`: {"version", "contract", "provider", "acqd": {path, len, mtime_ns} | null}, plus "acqd_absent" (why) when acqd is null; `--version` is the human form of the first two
  jobs          The live jobs: id, parent, kind, target (from params, C7), state (`↻n` counts 429 re-queues, C26), priority, account, submitter, ETA
  status        One job's state and ETA. A large ETA is the limiter holding, not a hang: holds can reach 300 s plus the timing bucket
  result        A finished job's payload or error, answered across daemon restarts (C27: a client that disappears leaves its jobs running). A failed fetch's refused body is in `acq store refused <id>`, not here
  cancel        Cancel a waiting or running job; cascades to every descendant still waiting (C23)
  set-priority  Change a waiting job's priority; higher runs sooner and the queue reorders live (C5)
  price         The listing state (no daemon): what every item, tab and character is priced as — by hand, in game, and how the two stand
  reference     Reference data the binary ships (no store, no daemon): the currency table, by version
  shop          The forum shop (no daemon, sends nothing): render the page set a hand price would post, every omission counted with its reason
  daemon        Debugging only — normal use never needs manual lifecycle
  help          Print this message or the help of the given subcommand(s)

Options:
      --json               Emit structured JSON instead of human-readable output
      --account <ACCOUNT>  Which account to act as: a username (with or without `#…`) or uuid. Defaults to `ACQ_ACCOUNT`, else the sole known/logged-in account [env: ACQ_ACCOUNT=]
  -h, --help               Print help
  -V, --version            Print version
```

## `acq auth`

```text
Log in via OAuth (mock provider, or real GGG with ACQ_GGG=1). The login completes only once its own profile job lands the account uuid (C50); the mock's page accepts any username, so a second account is one login apart

Usage: acq auth [OPTIONS] [COMMAND]

Commands:
  status  Show session state (login, token expiry, keyring health)
  check   Verify the session actually works: forces a token round-trip through the provider instead of trusting local state. Exit code 1 on failure
  logout  Drop the session and clear the keyring entry. With --account naming another known account, clear only that account's keyring entry
  help    Print this message or the help of the given subcommand(s)

Options:
      --no-browser         Print the login URL instead of opening a browser
  -h, --help               Print help
```

## `acq auth status`

```text
Show session state (login, token expiry, keyring health)

Usage: acq auth status [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq auth check`

```text
Verify the session actually works: forces a token round-trip through the provider instead of trusting local state. Exit code 1 on failure

Usage: acq auth check [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq auth logout`

```text
Drop the session and clear the keyring entry. With --account naming another known account, clear only that account's keyring entry

Usage: acq auth logout [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq profile`

```text
The account profile (account:profile)

Usage: acq profile [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq characters`

```text
List characters on the logged-in account. A route's first use queues a visible `probe` job — one HEAD that learns the policy and the account's current counters before anything real is sent (C20)

Usage: acq characters [OPTIONS]

Options:
      --realm <REALM>      pc (default), xbox, sony, or poe2. pc is omitted on the wire (C58); another realm is its own route segment, with its own probe [default: pc]
  -h, --help               Print help
```

## `acq character`

```text
Fetch one character with its equipment and inventory

Usage: acq character [OPTIONS] <NAME>

Arguments:
  <NAME>  

Options:
      --realm <REALM>      pc (default), xbox, sony, or poe2; pc is omitted on the wire (C58) [default: pc]
  -h, --help               Print help
```

## `acq leagues`

```text
List the account's leagues

Usage: acq leagues [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq accounts`

```text
Accounts this machine has logged into (the store's index; no daemon)

Usage: acq accounts [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq tabs`

```text
Tabs of a league, from the shared store (no daemon round-trip)

Usage: acq tabs [OPTIONS]

Options:
      --league <LEAGUE>    [default: Standard]
      --realm <REALM>      pc (default), xbox, or sony — stashes are PoE1 only (C59) [default: pc]
  -h, --help               Print help
```

## `acq items`

```text
Items in the shared store

Usage: acq items [OPTIONS] <COMMAND>

Commands:
  search  Substring search over name, type line, and base type
  show    One item by id, verbatim
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help               Print help
```

## `acq items search`

```text
Substring search over name, type line, and base type

Usage: acq items search [OPTIONS] <TEXT>

Arguments:
  <TEXT>  

Options:
      --league <LEAGUE>    
      --realm <REALM>      Restrict to one realm (pc, xbox, sony, poe2)
      --removed            Include items no longer seen at their last location
      --limit <LIMIT>      [default: 50]
  -h, --help               Print help
```

## `acq items show`

```text
One item by id, verbatim

Usage: acq items show [OPTIONS] <ID>

Arguments:
  <ID>  

Options:
  -h, --help               Print help
```

## `acq store`

```text
The shared store itself (what the daemon writes; every frontend reads)

Usage: acq store [OPTIONS] <COMMAND>

Commands:
  status      Path, size, and row counts
  characters  Characters on record (no daemon): id, address, league, freshness, live item count. `acq characters` (the job) fetches the list anew
  events      Item events (added/moved/changed/removed) from recent ingests: by default one line per location with counts (text) or the event list (--json); --expand / --summary pick either form in both modes
  refused     Bodies the store refused as malformed, kept verbatim as evidence: the list (newest first), or one body in full by its row id
  rebuild     Re-extract derived columns from each item's own JSON
  import      Replay a snapshot file from the retired `acq pull` into the store (no GGG traffic)
  help        Print this message or the help of the given subcommand(s)

Options:
  -h, --help               Print help
```

## `acq store status`

```text
Path, size, and row counts

Usage: acq store status [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq store characters`

```text
Characters on record (no daemon): id, address, league, freshness, live item count. `acq characters` (the job) fetches the list anew

Usage: acq store characters [OPTIONS]

Options:
      --realm <REALM>      Restrict to one realm (pc, xbox, sony, poe2); every realm otherwise
      --league <LEAGUE>    Restrict to one league; every league otherwise
  -h, --help               Print help
```

## `acq store events`

```text
Item events (added/moved/changed/removed) from recent ingests: by default one line per location with counts (text) or the event list (--json); --expand / --summary pick either form in both modes

Usage: acq store events [OPTIONS]

Options:
      --hours <HOURS>      [default: 24]
      --limit <LIMIT>      [default: 200]
      --expand             Every event, one per line (the JSON default)
      --summary            One line per location with counts (the text default)
  -h, --help               Print help
```

## `acq store refused`

```text
Bodies the store refused as malformed, kept verbatim as evidence: the list (newest first), or one body in full by its row id

Usage: acq store refused [OPTIONS] [ID]

Arguments:
  [ID]  Row id from the list (or from the failed job's error)

Options:
      --limit <LIMIT>      [default: 50]
  -h, --help               Print help
```

## `acq store rebuild`

```text
Re-extract derived columns from each item's own JSON

Usage: acq store rebuild [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq store import`

```text
Replay a snapshot file from the retired `acq pull` into the store (no GGG traffic)

Usage: acq store import [OPTIONS] <PATH>

Arguments:
  <PATH>  

Options:
  -h, --help               Print help
```

## `acq stashes`

```text
List stash tabs for a league: a second rate-limit policy, paced in parallel with the character routes

Usage: acq stashes [OPTIONS]

Options:
      --league <LEAGUE>    [default: Standard]
      --realm <REALM>      pc (default), xbox, or sony — the stash endpoints are PoE1 only; poe2 is refused at admission (C59) [default: pc]
  -h, --help               Print help
```

## `acq stash`

```text
Fetch one stash tab (or one substash of a map/unique tab)

Usage: acq stash [OPTIONS] <ID>

Arguments:
  <ID>  

Options:
      --sub <SUB>          
      --deep               Follow a map/unique tab's substashes as child jobs. Opt-in per tab: one map tab can hold hundreds
      --league <LEAGUE>    [default: Standard]
      --realm <REALM>      pc (default), xbox, or sony — the stash endpoints are PoE1 only; poe2 is refused at admission (C59) [default: pc]
  -h, --help               Print help
```

## `acq refresh`

```text
Refresh tabs: one stash-list request, then one `stash` child job per selected tab. Selection is explicit — there is no default. (The ad-hoc `--tabs`/`--all` kind and the plan path are two doors to one task; C76 rules the direction.)

Usage: acq refresh [OPTIONS]

Options:
      --all
          Every tab in the league (folder children included, folders not)

      --json
          Emit structured JSON instead of human-readable output

          Which account to act as: a username (with or without `#…`) or uuid. Defaults to `ACQ_ACCOUNT`, else the sole known/logged-in account
          
          [env: ACQ_ACCOUNT=]

      --tabs <TABS>
          Comma-separated tab ids

      --deep
          Also follow map/unique substashes (per tab, as child jobs)

      --plan[=<FILE>]
          Compile the stored sync policy (`acq policy`) into the explicit action set and print it — nothing is submitted or sent. A running daemon adds its read-only quote. With --json, prints the serializable plan envelope. `--plan=FILE` (or `=-` for stdin) renders a reviewed envelope instead, through the same renderer, with the quote it carries

      --expand
          With --plan: one line per action and every quote note, instead of the grouped view (groups over ten entities are counted)

      --apply[=<FILE>]
          Execute the plan: exactly its actions, as one `apply` parent job that never expands the set. Bare `--apply` compiles the stored policy now; `--apply=FILE` (or `--apply=-` for stdin) reads a reviewed plan envelope from `refresh --plan --json`. Refused if the stored policy revision no longer matches the plan's

      --max-requests <MAX_REQUESTS>
          Refuse the apply before anything runs if the plan authorizes more than this many requests (checked by the daemon at admission, before any child job exists)

      --league <LEAGUE>
          Defaults to Standard. With `--apply=FILE`, the plan's own league governs; giving --league too asserts they agree

      --realm <REALM>
          pc (default), xbox, sony, or poe2 (a poe2 policy entry can cover characters only). With `--apply=FILE`, the plan's own realm governs; giving --realm too asserts they agree

  -h, --help
          Print help (see a summary with '-h')

Reading a plan as an agent: the text is a function of the envelope (C53), so count with `jq` over `acq refresh --plan --json` rather than parsing prose —
  jq '.logical_requests'
  jq '[.actions[] | .action] | group_by(.) | map({(.[0]): length}) | add'          # requests by kind
  jq '[.actions[] | select(.action == "fetch_substash") | .parent] | group_by(.) | map({(.[0]): length}) | add'   # substashes per parent
  jq '[.skipped_tabs[], .skipped_characters[] | .reason.kind] | group_by(.) | map({(.[0]): length}) | add'   # skips by reason
`acq refresh --apply --json` adds `store_changes` beside the outcome; `acq store events --summary --json` is the per-location summary.
```

## `acq policy`

```text
The per-account sync policy: the declared coverage and freshness that `acq refresh --plan` compiles into requests

Usage: acq policy [OPTIONS] [COMMAND]

Commands:
  show  Print the stored policy value and its revision (the default)
  set   Write the policy: inline JSON, `-` to read stdin, or `@<path>` to read a file. Validated before it is stored — a typo'd field or an unknown version is refused, never half-honored
  help  Print this message or the help of the given subcommand(s)

Options:
  -h, --help               Print help
```

## `acq policy show`

```text
Print the stored policy value and its revision (the default)

Usage: acq policy show [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq policy set`

```text
Write the policy: inline JSON, `-` to read stdin, or `@<path>` to read a file. Validated before it is stored — a typo'd field or an unknown version is refused, never half-honored

Usage: acq policy set [OPTIONS] <VALUE>

Arguments:
  <VALUE>  

Options:
      --if-revision <IF_REVISION>  Only write if the stored policy is at exactly this revision (what `acq policy show` printed when you reviewed it); refused with the current revision otherwise. Without it the write replaces whatever revision is currently stored — though a write racing in between still conflicts rather than being clobbered
  -h, --help                       Print help
```

## `acq submit`

```text
Submit any job kind by hand

Usage: acq submit [OPTIONS] <KIND>

Arguments:
  <KIND>  A network kind — profile, characters, character, leagues, stashes, stash, refresh, apply — or a mock-only one: sleep, fetch, whoami (refused in real mode)

Options:
      --params <PARAMS>      JSON params, e.g. '{"seconds": 5}' [default: {}]
      --priority <PRIORITY>  Higher runs sooner. Default 0 [default: 0]
      --detach               Return the job id immediately instead of blocking with progress
  -h, --help                 Print help
```

## `acq demo`

```text
Submit a burst of fetch jobs against the mock's 5-per-10 s policy and watch the rate limiter queue them (the ETAs are the limiter's prediction, corrected by headers)

Usage: acq demo [OPTIONS]

Options:
      --count <COUNT>      [default: 8]
  -h, --help               Print help
```

## `acq dash`

```text
Live dashboard (TUI): rate limiter state (enter expands a policy: bucket state, the observed X-Rate-Limit headers, per-endpoint sends), job queue, HTTP sends, recent errors, a rails halt in red. With --json, prints one snapshot and exits

Usage: acq dash [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq version`

```text
What this build is (C84): the package version, the shared-contract revision the daemon handshake compares — a digest over the protocol and store crates, the root manifest and the lock, never a git commit — and the sibling `acqd` a job command would start, as found on disk (path, length, modification time; no hash — the run record hashes it). This reports the candidate; `acq daemon status` reports the daemon running. `--json`: {"version", "contract", "provider", "acqd": {path, len, mtime_ns} | null}, plus "acqd_absent" (why) when acqd is null; `--version` is the human form of the first two

Usage: acq version [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq jobs`

```text
The live jobs: id, parent, kind, target (from params, C7), state (`↻n` counts 429 re-queues, C26), priority, account, submitter, ETA

Usage: acq jobs [OPTIONS]

Options:
      --watch              Subscribe, print the queue, then every job-state change as it happens; the queue is printed again after a missed-events signal (C85), and the watch ends when the daemon stops
  -h, --help               Print help
```

## `acq status`

```text
One job's state and ETA. A large ETA is the limiter holding, not a hang: holds can reach 300 s plus the timing bucket

Usage: acq status [OPTIONS] <ID>

Arguments:
  <ID>  

Options:
  -h, --help               Print help
```

## `acq result`

```text
A finished job's payload or error, answered across daemon restarts (C27: a client that disappears leaves its jobs running). A failed fetch's refused body is in `acq store refused <id>`, not here

Usage: acq result [OPTIONS] <ID>

Arguments:
  <ID>  

Options:
  -h, --help               Print help
```

## `acq cancel`

```text
Cancel a waiting or running job; cascades to every descendant still waiting (C23)

Usage: acq cancel [OPTIONS] <ID>

Arguments:
  <ID>  

Options:
  -h, --help               Print help
```

## `acq set-priority`

```text
Change a waiting job's priority; higher runs sooner and the queue reorders live (C5)

Usage: acq set-priority [OPTIONS] <ID> <PRIORITY>

Arguments:
  <ID>        
  <PRIORITY>  

Options:
  -h, --help               Print help
```

## `acq price`

```text
The listing state (no daemon): what every item, tab and character is priced as — by hand, in game, and how the two stand

Usage: acq price [OPTIONS] [COMMAND]

Commands:
  status  One line on the league's listing state and the next action (the default); --expand adds the game side, the rows and the basis
  show    One target's listing: `item/<id>`, `character/<id>`, `tab/<realm>/<id>` or `substash/<realm>/<parent>/<id>` — both sides with their causes, the raw note beside the parse; a container's items summarized under it (--expand lists them all)
  list    Listed items grouped by container, ten or fewer listed per group and more counted (--expand lists every one with its texts). With neither --relation nor --effective, items whose relation is `none` are left out — except unresolved ones (a row that cannot be read could decide), which are always listed
  set     Set one target's price by hand — a row on an item, tab, substash or character; what it covers inherits it (C70). Prints what it replaced, and how to put that back
  clear   Remove one target's own price row; what it covered falls back to the next row up (C70). Prints what it removed and the command that puts it back
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help               Print help
```

## `acq price status`

```text
One line on the league's listing state and the next action (the default); --expand adds the game side, the rows and the basis

Usage: acq price status [OPTIONS]

Options:
      --league <LEAGUE>    [default: Standard]
      --realm <REALM>      
      --expand             
  -h, --help               Print help
```

## `acq price show`

```text
One target's listing: `item/<id>`, `character/<id>`, `tab/<realm>/<id>` or `substash/<realm>/<parent>/<id>` — both sides with their causes, the raw note beside the parse; a container's items summarized under it (--expand lists them all)

Usage: acq price show [OPTIONS] <TARGET>

Arguments:
  <TARGET>  

Options:
      --league <LEAGUE>    [default: Standard]
      --realm <REALM>      The realm of an item or character target; a tab or substash address carries its own
      --expand             
  -h, --help               Print help
```

## `acq price list`

```text
Listed items grouped by container, ten or fewer listed per group and more counted (--expand lists every one with its texts). With neither --relation nor --effective, items whose relation is `none` are left out — except unresolved ones (a row that cannot be read could decide), which are always listed

Usage: acq price list [OPTIONS]

Options:
      --league <LEAGUE>          [default: Standard]
      --realm <REALM>            The realm; a tab or substash address given to --in or --covered-by carries its own
      --relation <RELATION>      none, manual_only, game_only, agree or conflict
      --effective <EFFECTIVE>    Who decides (C81): game, manual, none or unresolved
      --in <LOCATION>            Only items physically in this container (a tab, substash or character address)
      --covered-by <COVERED_BY>  Only items a row on this target would cover (C70): a folder's tabs' items, a map or unique tab's substashes' items too
      --expand                   
  -h, --help                     Print help
```

## `acq price set`

```text
Set one target's price by hand — a row on an item, tab, substash or character; what it covers inherits it (C70). Prints what it replaced, and how to put that back

Usage: acq price set [OPTIONS] <TARGET> <TYPE> [AMOUNT] [CURRENCY]

Arguments:
  <TARGET>    `item/<id>`, `character/<id>`, `tab/<realm>/<id>` or `substash/<realm>/<parent>/<id>`
  <TYPE>      exact, negotiable, no_price or skip (the game's `price`, `b/o` and `~skip` work too)
  [AMOUNT]    For exact and negotiable: a decimal of up to four places (`12.5`), or a `wanted/lot` ratio (`1/5`)
  [CURRENCY]  For exact and negotiable: a tag of `acq reference currency`

Options:
      --if-revision <IF_REVISION>  Only write over exactly this revision (what `acq price show` printed when you reviewed it); refused naming the current one otherwise. Without it the write replaces whatever is stored — though a write racing in between still conflicts rather than being clobbered
  -h, --help                       Print help
```

## `acq price clear`

```text
Remove one target's own price row; what it covered falls back to the next row up (C70). Prints what it removed and the command that puts it back

Usage: acq price clear [OPTIONS] <TARGET>

Arguments:
  <TARGET>  The address, as for `set`

Options:
      --if-revision <IF_REVISION>  Only clear exactly this revision; see `set`
  -h, --help                       Print help
```

## `acq reference`

```text
Reference data the binary ships (no store, no daemon): the currency table, by version

Usage: acq reference [OPTIONS] <COMMAND>

Commands:
  currency  The currency table: every row (tag, display name, the words a parser accepts, retired marks), or one word resolved. Exact and case-sensitive; `--json` is the whole table with its evidence
  help      Print this message or the help of the given subcommand(s)

Options:
  -h, --help               Print help
```

## `acq reference currency`

```text
The currency table: every row (tag, display name, the words a parser accepts, retired marks), or one word resolved. Exact and case-sensitive; `--json` is the whole table with its evidence

Usage: acq reference currency [OPTIONS] [WORD]

Arguments:
  [WORD]  A tag, the game's word, or a legacy alias to resolve

Options:
      --expand             Show each row's evidence and the table's sources
  -h, --help               Print help
```

## `acq shop`

```text
The forum shop (no daemon, sends nothing): render the page set a hand price would post, every omission counted with its reason

Usage: acq shop [OPTIONS] <COMMAND>

Commands:
  render  Render the shop pages to stdout for pasting by hand (C74): one link code per hand-priced item under its price's spoiler, one page spoiler labelled n of N, in pages of at most --size characters; what the game already lists is omitted and counted, an unobserved case is blocked and counted; the sync policy's coverage and freshness are reported, never enforced (C72)
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help               Print help
```

## `acq shop render`

```text
Render the shop pages to stdout for pasting by hand (C74): one link code per hand-priced item under its price's spoiler, one page spoiler labelled n of N, in pages of at most --size characters; what the game already lists is omitted and counted, an unobserved case is blocked and counted; the sync policy's coverage and freshness are reported, never enforced (C72)

Usage: acq shop render [OPTIONS]

Options:
      --league <LEAGUE>      [default: Standard]
      --realm <REALM>        
      --size <SIZE>          Characters per page, the template included (the forum's hard limit, T23) [default: 50000]
      --template <TEMPLATE>  A file whose `[items]` token each page replaces; without it the page is the items alone
      --page <PAGE>          Print one page alone (its text; with --json, its record), for the clipboard
      --expand               The whole policy table with each row's rule and count, and every item left off the page with its cell
  -h, --help                 Print help
```

## `acq daemon`

```text
Debugging only — normal use never needs manual lifecycle

Usage: acq daemon [OPTIONS] <COMMAND>

Commands:
  status          The daemon running: pid, version, contract revision, the executable it runs from and its hash (C84), provider, uptime, connections, queue counts, policies learned, the socket, log and journal paths, the rails state, keyring health. Observes only (C10): never spawns or replaces; a daemon of another contract, artifact or provider is reported by its identity alone — no vitals — and left running. `--json`, in both cases: running, compatible, which of the three dimensions match (`contract_matches`, `artifact_matches`, `provider_matches`), how the daemon's file relates to this client's sibling — `artifact_relation`: `same_file`; `same_bytes`, another copy; `different`; `unhashable`; `no_sibling`, no `acqd` beside this executable; `unreported`, a daemon from before the field — and under `wanted` this client's own version, contract and provider with the sibling `acqd` a job command would start, all from the one look that judged the daemon
  stop            Stop the daemon that is listening, this build's — its contract and artifact — or another's. Queued jobs stay on disk and resume under the next one (C6); a client's jobs are never cancelled by its leaving (C27)
  reset-tripwire  Clear the live-test rails' tripwire/ceiling halt (see LIVE-TESTING.md). Observe the post-violation rule before using this
  help            Print this message or the help of the given subcommand(s)

Options:
  -h, --help               Print help
```

## `acq daemon status`

```text
The daemon running: pid, version, contract revision, the executable it runs from and its hash (C84), provider, uptime, connections, queue counts, policies learned, the socket, log and journal paths, the rails state, keyring health. Observes only (C10): never spawns or replaces; a daemon of another contract, artifact or provider is reported by its identity alone — no vitals — and left running. `--json`, in both cases: running, compatible, which of the three dimensions match (`contract_matches`, `artifact_matches`, `provider_matches`), how the daemon's file relates to this client's sibling — `artifact_relation`: `same_file`; `same_bytes`, another copy; `different`; `unhashable`; `no_sibling`, no `acqd` beside this executable; `unreported`, a daemon from before the field — and under `wanted` this client's own version, contract and provider with the sibling `acqd` a job command would start, all from the one look that judged the daemon

Usage: acq daemon status [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq daemon stop`

```text
Stop the daemon that is listening, this build's — its contract and artifact — or another's. Queued jobs stay on disk and resume under the next one (C6); a client's jobs are never cancelled by its leaving (C27)

Usage: acq daemon stop [OPTIONS]

Options:
  -h, --help               Print help
```

## `acq daemon reset-tripwire`

```text
Clear the live-test rails' tripwire/ceiling halt (see LIVE-TESTING.md). Observe the post-violation rule before using this

Usage: acq daemon reset-tripwire [OPTIONS]

Options:
  -h, --help               Print help
```

