# generated by scripts/query-plans.py from crates/acquisition-store/src/schema.sql (schema applied to an empty
# in-memory database) and the queries copied verbatim from lib.rs / snapshot.rs.
# sqlite 3.53.4; EXPLAIN QUERY PLAN, no data, no timings.

## Store::search — acq items search / MCP search_items
   source: lib.rs Store::search
   SCAN items USING INDEX items_location
   USE TEMP B-TREE FOR LAST 2 TERMS OF ORDER BY

## the same predicate anchored (no leading wildcard) — what an index could serve
   source: not in the crate: the counterfactual
   SCAN items

## Store::item — acq items show / MCP get_item
   source: lib.rs Store::item
   SEARCH items USING INDEX sqlite_autoindex_items_1 (id=?)

## Store::tabs — acq tabs / MCP tabs (the per-tab live item count is a correlated subquery)
   source: lib.rs Store::tabs
   SEARCH t USING INDEX sqlite_autoindex_tabs_1 (realm=? AND league=?)
   CORRELATED SCALAR SUBQUERY 1
   SEARCH i USING INDEX items_location (location_kind=? AND location_id=?)
   CORRELATED SCALAR SUBQUERY 2
   SEARCH p USING INDEX sqlite_autoindex_tabs_1 (realm=? AND league=? AND id=?)
   USE TEMP B-TREE FOR ORDER BY

## Store::characters — acq store characters / MCP characters
   source: lib.rs Store::characters
   SCAN c
   CORRELATED SCALAR SUBQUERY 1
   SEARCH i USING INDEX items_location (location_kind=? AND location_id=?)
   CORRELATED SCALAR SUBQUERY 2
   SCAN json_each VIRTUAL TABLE INDEX 1:
   USE TEMP B-TREE FOR ORDER BY

## Store::events_since — acq store events / MCP item_events
   source: lib.rs Store::events_since
   SEARCH e USING INDEX item_events_at (at>?)
   SEARCH i USING INDEX sqlite_autoindex_items_1 (id=?) LEFT-JOIN

## read_items — the item body of PricingSnapshot (acq price, acq shop)
   source: snapshot.rs read_items
   MULTI-INDEX OR
   INDEX 1
   CORRELATED SCALAR SUBQUERY 1
   SEARCH t USING INDEX sqlite_autoindex_tabs_1 (realm=? AND league=? AND id=?)
   SEARCH i USING INDEX items_location (location_kind=?)
   INDEX 2
   CORRELATED SCALAR SUBQUERY 2
   SEARCH c USING INDEX sqlite_autoindex_characters_1 (id=?)
   SEARCH i USING INDEX items_location (location_kind=?)
   USE TEMP B-TREE FOR ORDER BY

## the location filter alone — what items_location serves
   source: not in the crate: the counterfactual
   SEARCH items USING INDEX items_location (location_kind=? AND location_id=?)

## stash_basis — the listing a plan cites
   source: snapshot.rs stash_basis
   SCAN responses

## exact name — the only shape items_names can serve (no crate read does this)
   source: not in the crate: the counterfactual
   SEARCH items USING INDEX items_names (name=?)

## anchored LIKE under case_sensitive_like=ON — why the default scans
   source: not in the crate: the counterfactual (pragma set for this query only)
   SEARCH items USING INDEX items_names (name>? AND name<?)

## a whole league's live items — no index spans (realm, league, removed_at)
   source: not in the crate: the counterfactual
   SCAN items
